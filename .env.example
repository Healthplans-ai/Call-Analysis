# Copy to ".env" (or set in Vercel project settings).

# Base URL of the backend deployed on Railway (no trailing slash).
VITE_API_BASE_URL=http://localhost:8000

# Shared API key — must match the backend API_KEY.
VITE_API_KEY=p3-dev-3f9a2c7e54b14d8fae6190b7c2d0a1e8
